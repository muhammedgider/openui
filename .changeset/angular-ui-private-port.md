---
---

Add an in-progress, private Angular UI catalog package, its playground scenarios and regression tests. This package does not publish to npm yet and does not change the runtime API of any existing published package. Release/version policy and complete visual parity remain pending.
